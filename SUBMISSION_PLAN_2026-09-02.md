# Alpha Council — Pre-Submission Plan & Recommended Modifications

**Written:** 2026-09-01, ~23:15 ET (after the feature-completion push, before the Sep 2 session)
**Review:** Sep 2 after the 16:15 post-close cycle, together with the day's data
**Status:** R1–R5 were **accepted by the operator and implemented the same night** (see Part 3 for what shipped). **R6–R8 remain untouched**, deferred to the Sep 2 evening review per the spec's discipline (§20.3: at most one quality-gate change per day, hard gates never; §22 Phase E: no refactors on the last trading day). No quality gate has moved: R1/R2 are operational tooling, R3/R4 are spec-sanctioned config, R5 is wording.

**Clock:** trading sessions remain Sep 2 and Sep 3 (flatten 15:45 ET Sep 3). Submission closes **Sep 4, 11:00 ET** — the morning after the last session, so anything not finished by Sep 3 evening is being finished under deadline pressure.

---

## 0. State of play (verified tonight, not assumed)

| Fact | Evidence |
|---|---|
| Broker account is **flat** — nothing at risk overnight | Live `/v2/positions` query: 0 positions |
| Three calibration lifecycles **filled at the broker on Aug 31** (opens at 3.99 and 6.03; an mleg close at **−6.05 net credit**; two single-leg closes) | Live `/v2/orders` history |
| …but the local books captured almost none of it | DB: 1 order row, 1 open-side calibration row, **0 trade_journal rows, 0 shadow trades, 0 closes, 0 attribution** |
| The Claude Red Team has **never executed live** | `red_team_reviews` empty; Anthropic spend $0.00 |
| Both councils that ran ended at PM abstain | 2 proposals, `trade=0`, confidence 0.82/0.87; reasons: (1) bearish signal vs bullish event, (2) no catalyst/novelty + no invalidation level |
| Candidates now clear the floor | 151 scored: max final 67.6, **26 ≥ 62** (tier-1 floor), 82 in the 55–62 band |
| The tape has been bearish-leaning | EVENT/BEARISH avg 61.9 (hugging the floor), MOMENTUM/BEARISH n=69 |
| Regime factor is not mass-zeroing (earlier concern retired) | 95 candidates at 1.0, 40 at 0.8, only 1 zeroed |
| AI budget is barely touched | $0.52 of $50 OpenAI; $0.00 of $50 Anthropic |
| **Git history is clean of secrets** — safe to take public | Exact-path `.env` never committed (only `.env.example`); key-pattern sweep over history clean |
| `README.md` is **empty (0 bytes)** | `wc -c` |
| Spec-named operational scripts missing | `close_all.py` (§18.2 says it must exist and be manually runnable), `gate_report.py`, `dry_run_trade.py`, `scan_once.py` (the last three are covered by existing flags/dashboard) |
| Negative-limit close convention confirmed against the venue | The Aug 31 mleg close filled at −6.05 (net credit) — matching the close-walk implementation shipped tonight |

The one-sentence summary: **the machine is built and safe, but the differentiator has no data yet.** Counterfactual Lab, Execution Quality, GateValue-on-shadow-marks, and the Red Team's live verdict history are all empty. Two sessions remain to fill them.

---

## Part 1 — Hard submission requirements (independent of trading results)

These consume real hours and none of them can be generated by the trading loop. Suggested owner-time: Sep 2 evening for drafts, Sep 3 evening for finals (per spec Phase E: "Evening: README, diagram, screenshots, video").

1. **README.md — currently empty.** The single largest writing task. Must cover (per DoD §30): what Alpha Council is; the differentiator (Counterfactual Decision Attribution + Gate Attribution + Champion/Challenger); architecture (funnel 250→30→20→5→≤3 councils; deterministic-decides / AI-proposes); an **accurate** IEX + Indicative-feed section (measured quote lags, delta adjustment, fill calibration — not "everything is 15 minutes delayed"); setup/run instructions; the honest-framing paragraph from §20.3(c) — *the calibration/evolution loop is presented as instrumentation that would enable learning at scale; the competition run is its first, deliberately small sample*. Draft can be written Sep 2 evening from the review doc + specs.
2. **Architecture diagram** — the §3 ASCII diagram redrawn as one clean image (the brand art style is optional; clarity is not).
3. **Screenshots** — dark-mode dashboard: Command Center (hero), Discovery Funnel, Council Decision with a real decision, Gate Lab, Alpha Evolution with the champion + lessons; **Counterfactual Lab and Execution Quality after the first tracked close** (see Part 2 — they are empty until then). One light-mode shot to show the toggle.
4. **Demo video** — script it from §27 + v2.5 §26's twelve-step sequence, ending on "promotion remains blocked because evidence is insufficient" as the deliberate closing beat. Record Sep 3 evening when the data is final.
5. **Submission write-up** — map explicitly to the four judging criteria using v2.5 §2's table; include the spend figures (≈$1 of $100 used) as an efficiency point.
6. **Repo public flip + final hygiene pass** — history verified clean tonight; before flipping: re-run the key-pattern sweep, confirm `.env`/`data/` still untracked, and skim the issue/PR tabs are empty of anything private.
7. **Database evidence snapshots** — copy `data/alpha_council.db` after each close (`alpha_council_2026-09-02.db`, `-03.db`) outside the repo. The DB is the audit trail behind every claim; keep it even though it isn't submitted.
8. **Submission-form logistics** — account, team name, category answers, video hosting. Do the dry run of the form on Sep 3 afternoon, not Sep 4 morning.

---

## Part 2 — Operational musts during the remaining sessions (beyond "run it")

### 2.1 The tracked-lifecycle gap — the highest-stakes item on this page
The DoD requires ≥6 complete order lifecycles and the demo requires the attribution/execution tabs to show real rows. Aug 31's three lifecycles happened **outside the journaled path** (vertical_slice submitted and recorded calibration, but no journal/shadow/close rows exist), so they cannot honestly power the demo. Plan:

- **Sep 2, mid-morning, supervised:** run 1–2 CALIBRATION-track lifecycles on SPY/QQQ (1-lot, per `activity_target`) **through the journaled path** so trade_journal, shadow EXECUTED variant, close, execution calibration (open **and** close side — new tonight), and attribution all populate. See R2 — the current `vertical_slice.py` needs a small verification/extension first, which is why this is also a recommendation and not just an instruction.
- Alpha trades then land on top if they qualify; with 26 candidates ≥ floor and both historical abstain causes now addressed (scenario payoff tables supply invalidation levels; the intel-contradiction gate pre-empts the bearish-vs-bullish-event abstain), the odds are materially better than Monday's.
- Target arithmetic: 3 untracked (Aug 31, disclose honestly) + ≥3 tracked across Sep 2–3 ≥ 6 total, with the tracked ones carrying the demo.

### 2.2 First live day for last night's features — watch list
- 08:45 `PREMARKET_BRIEF` event (failure logs `PREMARKET_FAILED`, scanning continues).
- `SEC_COLLECTED` counts per scan **and** that recalibrated 424B/Form-4 events stay below the material bar in RTH (`SELECT event_type, AVG(catalyst_score), AVG(materiality_score) FROM intelligence_events WHERE event_type LIKE 'sec%' AND created_at >= <today>`).
- `NEWS_INJECTION` count — off-universe symbols entering via material news.
- First §17.4 pre-submit refresh on a real submission (look for `EXEC_STALE_PRESUBMIT`/`EXEC_REPRICE_FAILED` rejections — their *absence* on a filled order is the pass).
- Post-close: lessons rows, then either `CHALLENGER_STORED` or an explicit `NO_CHANGE` (19 decisions already ≥ the 8-observation floor, so a proposal is likely — a demo asset either way).
- Anthropic spend > $0 for the first time = the Red Team actually ran.

### 2.3 Sep 3 flatten assurance
The automatic flatten depends on one process being alive at 15:45. The missing `close_all.py` (R1) is the insurance policy; until it exists, the fallback is the Alpaca web UI. Decide R1 tomorrow evening.

### 2.4 MCP share on demo days
`mcp_share` is measured and reported. Confirm MCP connects at Sep 2/3 startup (the banner warns loudly if not) — the hackathon requires MCP usage and the write-up will quote the measured share.

---

## Part 3 — Recommended modifications

**Status update (2026-09-01, late evening): R1–R5 were accepted and implemented the same night** (operator decision). R6–R8 remain deferred to the Sep 2 post-close review as originally planned.

### R1 — `scripts/close_all.py` — ✅ IMPLEMENTED
Standalone flatten runnable with no scheduler: cancels working orders, closes every journaled position through the full path (credit walk, close-side calibration, journal close, shadow freeze + attribution), names **broker-only orphans loudly** and closes them leg-by-leg with marketable limits (the Aug 31 lesson encoded), then re-verifies both books and exits non-zero if anything remains open. Dry-run by default; `--execute` to act; `--competition` journals closes as COMPETITION_FLATTEN.

### R2 — Journaled calibration lifecycle — ✅ IMPLEMENTED as `scripts/calibration_trade.py`
A purpose-built script rather than surgery on `vertical_slice.py` (which stays as the smoke-test probe): builds a real spread on a configured calibration symbol, runs the **Risk Constitution with every hard gate binding** (a new `is_calibration_trade` waiver covers only the PM-confidence and opportunity-score *quality* floors, which have no meaning for a PM-less lifecycle test — pinned by tests), does the §17.4 pre-submit refresh, walks the open, journals everything (decision states, trade_journal, EXECUTED shadow, both-side execution calibrations), holds, and closes through `monitor.close`. Ends with a row-by-row reconciliation printout. Dry-run by default; `--execute` for the real lifecycle; `--skip-close` leaves the position journaled and monitor-restorable. **Sep 2 mid-morning: run it once supervised (§2.1).**

### R3 — Session-open news lookback — ✅ IMPLEMENTED (config bump, 8h → 18h)
The 09:40 scan can now see the 16:00–01:40 overnight tape. Dedup keeps re-seen items idempotent; freshness decay already discounts old stories in scoring.

### R4 — Spend the surplus — ✅ IMPLEMENTED (config)
`red_team.evidence_cap_tokens: 8000 → 12000` (the spec's first-priority §14.3 upgrade) and `scenario_generator` restored to **Sol** (v2.5 §16's original assignment). Worst case ≈ +$0.05/council against $99.48 of unused budget.

### R5 — The MCP execution claim — ✅ DECIDED: language fix (option a)
Swept the repo: no code, docstring, dashboard, or runbook text claims MCP submits orders — only the spec's own demo narrative does, and specs are inputs, not claims we publish. **Canonical wording for the README, write-up, and video narration:**

> *Alpaca's MCP server is Alpha Council's control plane: account state, the market clock, and positions are served over MCP, with the measured MCP share of control-plane calls reported in the Audit tab. Order execution deliberately uses Alpaca's REST API directly, because the idempotent submit-verify-recover semantics around each `client_order_id` are load-bearing safety behavior we were not willing to route through a second transport during competition week.*

Option (b) — routing submission through MCP-with-REST-fallback — remains available for a **Sep 2 evening** decision only if the day produces multiple clean fills; never on Sep 3.

### R6 — Tier-1 final floor 62 → 60 (conditional; the one §20.3(a) quality-gate move available tomorrow)
*Evidence:* EVENT/BEARISH averages 61.9 — the strongest track sits *on* the floor; 82 candidates in the 55–62 band. But 26 already clear 62, so tomorrow may not need this.
*Decide by:* Sep 2 close. Accept **only if** the day shows repeated near-misses (final scores 58–62 dying at `FINAL_SCORE_FLOOR` while councils run under-quota) and zero alpha entries. One gate, one day, one `config_versions` row — the spec's calibration loop working as designed.

### R7 — Post-close portfolio attribution completeness
*Evidence:* attribution currently computes per decision; the portfolio roll-up view exists (`v_attribution_totals`) and the write-up will quote it. After the first tracked closes, verify the numbers reconcile against `trade_journal.realized_pnl` and screenshot then. Not a code change — a verification step with a fallback hour reserved Sep 3 evening if reconciliation surprises.

### R8 — Small polish items, only if Sep 2 is calm (else skip without regret)
- `structure_id` embeds rank-at-build (`st_SYM_1_…`) while `rank` is reassigned by `_diversify` — cosmetic mismatch in audit rows; 10-line fix.
- Dashboard: Gate Lab could label the DISC_* hygiene gates as "breadth hygiene" vs. quality gates for the judges' reading.
- `vertical_slice`'s stray CALIBRATION candidate rows score `final=0.0` in candidate_scores — harmless, but a `WHERE candidate_track != 'CALIBRATION'` on the scanner tab query would tidy the display.

---

## Part 4 — Explicitly NOT recommended before submission

For the record, so the review doesn't relitigate them:

- **No weight refits or threshold optimization from this sample** — §20.3(c) verbatim: with tens of decisions, fitting is noise-chasing; the honest framing (instrumentation, first sample) is worth more to judges than a fake optimizer. The Alpha Evolution challenger exists precisely to make this point *in shadow*.
- **No hard-gate changes of any kind** (2% per trade, 10% open, 4% sector, drawdown halts, liquidity floor, DTE, paper lock).
- **No new data sources, no Schwab, no Next.js, no per-rung §17.4 re-approval, no auto-promotion** — all explicitly out of scope in §28 / v2.5 §0.9.
- **No dashboard feature work after Sep 2** — spec freeze; Sep 3 is trade/monitor/record only.
- **No cutoff change** (15:35 + 15:20-scan pairing stands as the operator's coherent choice; both read one config key now).

---

## Part 5 — Sep 2 evening review agenda (30 minutes, in order)

1. **Did anything trade?** `trade_journal` rows; if zero alpha AND zero calibration → R2/2.1 failed, make Sep 3 morning calibration the first action of the day.
2. **Red Team first-run check:** `red_team_reviews` count, verdict mix, Anthropic spend. If still zero → the demo's governance layer needs Sep 3's sessions; prioritize anything that gets a council past the PM.
3. **Near-miss histogram for R6:** `SELECT COUNT(*) FROM gate_rejections WHERE gate_id='FINAL_SCORE_FLOOR' AND occurred_at >= <today>` plus the day's max final scores → accept/reject the 62→60 move (one gate, or none).
4. **New-feature watch list results** (2.2): premarket / SEC / injection / presubmit / evolution — anything degraded gets a targeted look, not a rewrite.
5. **R1–R5 are done** — instead: confirm R2's lifecycle actually ran during the day (§2.1), confirm R3/R4 behaved (bigger open-scan intel set; Red Team/scenario spend visible), and take the R5(b) go/no-go if the day produced multiple clean fills. Then **accept/reject R6** (floor 62→60) on the near-miss histogram, and triage R7–R8.
6. **Snapshot the DB**, draft the README from the review doc, screenshot whatever now has data.
7. **Set the Sep 3 plan:** start before 08:45, calibration lifecycle(s) if tracked count < 4, no code changes after market open, flatten verified 15:45, assets in the evening, submit Sep 4 by 10:00 ET with an hour of slack.

---

## Part 6 — Sep 2 evening: results against this plan

Recorded ~18:00 ET Sep 2, after the 16:15 post-close cycle. Agenda numbering follows Part 5.

1. **Did anything trade?** One CALIBRATION lifecycle through the full journaled path (SPY 759/772: open walked 3 rungs to 5.33 in 86s, close filled first credit rung 5.24 in 0.2s, realized **−$9.00**; both-side calibrations recorded: open bias +$0.13, close +$0.005). Zero alpha trades — every abstain/rejection was reasoned, none were system failures. §2.1's tracked-count target now stands at 1 of ≥3; **Sep 3 morning should run 1–2 more calibration lifecycles.**
2. **Red Team first-run:** 3 MODIFY verdicts (HL, EOSE, IREN) with substantive critiques; 6 Anthropic runs, $0.34 spend. Two early calls died at the 4096 default `max_tokens` (fixed to 16384 same day); every later call clean. The governance layer has live history for the demo.
3. **R6 near-miss histogram:** 26 `FINAL_SCORE_FLOOR` rejections on the day, but only **2 genuine near-misses** (61.1, 61.0 — both EVENT vs the 62 floor) and 4 candidates total in the 58–62 band, against 56 clearing 62. Councils were never under-quota because of the floor (the day-cap was the binding constraint until fixed). **R6's acceptance condition was not met**; the operator instead shipped a narrower, better-targeted change the same evening: EVENT candidates get their own per-tier `final_score_floor_event` (2 below momentum's bar) — commit `1881a41` — plus `RISK_PM_CONFIDENCE` now tests rev-0 conviction (commit `4615ff4`), addressing the IREN 0.51-vs-0.52 death directly. The Tier-1 62 floor itself did not move.
4. **New-feature watch list:** premarket brief ✅ (1 row, 08:45), SEC collector ✅ (11–22 filings/scan; recalibrated bands held after the 424B purge), news injection ✅ (17–54 symbols/scan), presubmit refresh ✅ (exercised by the calibration open), evolution post-close ✅ (3 lessons, explicit NO_CHANGE on one-calibration-trade evidence — the epistemically correct call and a demo asset). Shadow marks on an open alpha position remain unproven (none opened).
5. **R5(b)** (MCP-with-fallback execution): **no-go** — the day produced one clean fill pair, not "multiple"; language stands as written. **R7** (attribution reconciliation): 6 decision_attribution rows exist; totals are all zero as expected — a calibration trade has no GPT/Claude variants, so layer effects are structurally 0 while journal P&L is −$9.00; no discrepancy. Real reconciliation waits for the first alpha close. **R8**: untouched, still optional.
6. **Assets:** README written and pushed (`40cdeb6`), SUBMISSION_WRITEUP.md written and pushed, VIDEO_SCRIPT.md drafted local-only (gitignored per operator instruction), DB snapshot taken to `..\alpha-council-snapshots\alpha_council_2026-09-02.db`. Screenshots deferred to Sep 3 final data.
7. **Sep 3 plan stands** as written, with one addition: verify the five 09-02 evening live-patches (seat allocation, output_config effort, EVENT floor, rev-0 conviction floor, volume-gate stand-down) behave on the 09:40 scan before leaving the desk unattended.

---

*Companion documents: `CODE_REVIEW_2026-09-01.md` (issue register and feature build record, incl. the Part 8 session-morning checklist), `MONDAY_RUNBOOK_v3.md` (operational commands).*
