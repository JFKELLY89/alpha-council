
Wire news into scheduler, fix option_confidence keyword, move cutoff to 15:35

First autonomous session ran today. All six gates met: full pipeline

verified end to end including Claude returning MODIFY with an EXPRESSION

objection on a live candidate, plus two real paper fills. Zero autonomous

trades, for reasons now known rather than mysterious.

Scheduler wiring

- Pass intel_by_symbol and benchmark_return into FunnelScanner.run().

  Every scheduled scan was previously MOMENTUM-only with a null catalyst,

  which is the condition that produced five PM abstentions today.

- benchmark_return defaulted to 0.0, so regime_factor returned the neutral

  0.90 for every candidate. Observed scores of 62.1-62.8 were really 69-70

  before that multiplier, above the Tier 1 floor of 68.0. Note this cuts

  both ways: a real benchmark return zeroes candidates opposing the index

  without a Tier-1 catalyst, per spec 12.9. Behaviour to watch in the Gate

  Lab tomorrow.

- Add _gather_intelligence(): fetches snapshots and news for the discovery

  universe. Every failure path degrades to MOMENTUM rather than aborting

  the scan.

- Fix TypeError: Orchestrator.evaluate_candidate takes option_confidence,

  not option_data_confidence. Every scan that reached a candidate was

  dying here, which is why the 15:00 Tier 2 scan failed after the ladder

  correctly produced its first candidate of the day.

- Add news_lookback_hours: 8 to discovery config. A 12-hour window

  qualified nothing on freshness this morning; 6 hours qualified twenty.

Risk configuration

- Move new_trade_cutoff_et 15:20 -> 15:35, in both risk_constitution.yaml

  and the scheduler job. The 15:00 scan needs 30-60s for discovery,

  prescore, options and a council; at a 15:20 cutoff an approved trade

  from that scan is blocked on timing rather than on risk. 25 minutes

  remain before the close for the limit walk and several monitor polls.

  A hard gate changed deliberately, not to force activity.

Diagnostics

- Add scripts/check_alpaca.py: reads positions, orders and equity straight

  from the broker. Used today to find an orphaned SPY spread the local

  journal knew nothing about.

- Add scripts/check_funnel.py: scan_runs, discovery counts and funnel

  snapshots, which is what localised the candidate loss to the options

  prescreen stage.

Known open items

- prescore_survivors collapses to 0 on some scans while stage0 returns 30.

  Needs diagnosis; loosening a floor cannot reduce survivors.

- The decisions-row seeding in vertical_slice.py (5bd5d99) is present but

  unverified: no order has been submitted since it landed. An orphaned SPY

  765/773 call spread from 14:06 UTC, opened under the old code, is still

  open at the broker with nothing tracking it locally.

- MCP control plane connected at 12:54 with 54 tools resolved, then failed

  with Connection closed on a later run. Eligibility requirement, needs to

  be stable before Thursday.

- Anthropic spend reads $0.00 despite a completed Red Team call, so budget

  tracking for that provider is blind.

